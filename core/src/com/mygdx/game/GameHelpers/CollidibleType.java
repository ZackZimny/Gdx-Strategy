package com.mygdx.game.GameHelpers;

/**
 * keeps track of what type of Entity a collidible is attached to a playerUnit,
 * EnemyUnit, or Building
 **/
public enum CollidibleType {
  PlayerUnit,
  EnemyUnit,
  Building,
}
